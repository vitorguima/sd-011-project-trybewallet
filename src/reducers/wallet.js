const initialState = {
  currencies: [],
  expenses: [],
};

const wallet = (state = initialState, action) => {
  switch (action.type) {
  case 'MUDANCA':
  default:
    return state;
  }
};

export default wallet;
