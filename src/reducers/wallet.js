const INITIAL_STATE = {
  currencies: [],
  expenses: [],
  total: null,
};

const wallet = (state = INITIAL_STATE, { type }) => {
  switch (type) {
  default:
    return state;
  }
};

export default wallet;
