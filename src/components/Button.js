import React, { Component } from 'react';

class Button extends Component {
  render() {
    return (
      <div>
        <button type="button">Entrar</button>
      </div>
    );
  }
}

export default Button;

// Foi criado o botão do requisito 1.
